export interface IStopModel {
  stopId: string;
  stopCode: string;
  stopName: string;
  stopDesc: string;
  stopLat: number;
  stopLon: number;
  zoneId: string;
  stopUrl: string;
  locationType: number;
  parentStation: string;
}
